# VLC reference (read-only)

Do not vendor the Videolan tree into this repo.

Upstream: https://github.com/videolan/vlc

MVP desktop player uses system **libVLC / VLC** (`cvlc` + RC interface) from the OS package
(`apt install vlc libvlc-dev`). See `bgssai-media-desktop/`.
