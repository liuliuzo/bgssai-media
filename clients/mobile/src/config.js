/**
 * Runtime URLs for the single mobile shell.
 * Local defaults match README ports; override before sync for staging/prod.
 */
window.BGSSAI_MEDIA_SHELL = {
  userUrl: 'http://127.0.0.1:3002/login',
  adminUrl: 'http://127.0.0.1:3001/login',
  productName: 'BGSSAI Media',
};
