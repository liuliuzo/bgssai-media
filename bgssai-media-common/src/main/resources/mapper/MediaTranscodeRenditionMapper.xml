<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bgssai.media.common.mapper.MediaTranscodeRenditionMapper">
  <resultMap id="BaseResultMap" type="com.bgssai.media.common.domain.MediaTranscodeRendition">
    <id column="id" property="id"/>
    <result column="job_id" property="jobId"/>
    <result column="video_id" property="videoId"/>
    <result column="bitrate_label" property="bitrateLabel"/>
    <result column="height" property="height"/>
    <result column="play_url" property="playUrl"/>
    <result column="storage_path" property="storagePath"/>
    <result column="created_at" property="createdAt"/>
  </resultMap>

  <sql id="Base_Column_List">id, job_id, video_id, bitrate_label, height, play_url, storage_path, created_at</sql>

  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">and ${criterion.condition}</when>
                <when test="criterion.singleValue">and ${criterion.condition} #{criterion.value}</when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>

  <select id="selectByExample" resultMap="BaseResultMap">
    select <include refid="Base_Column_List"/>
    from media_transcode_rendition
    <if test="_parameter != null"><include refid="Example_Where_Clause"/></if>
    <if test="orderByClause != null">order by ${orderByClause}</if>
  </select>

  <select id="countByExample" resultType="long">
    select count(*) from media_transcode_rendition
    <if test="_parameter != null"><include refid="Example_Where_Clause"/></if>
  </select>

  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="long">
    select <include refid="Base_Column_List"/>
    from media_transcode_rendition where id = #{id}
  </select>

  <insert id="insertSelective" parameterType="com.bgssai.media.common.domain.MediaTranscodeRendition"
          useGeneratedKeys="true" keyProperty="id">
    insert into media_transcode_rendition
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="jobId != null">job_id,</if>
      <if test="videoId != null">video_id,</if>
      <if test="bitrateLabel != null">bitrate_label,</if>
      <if test="height != null">height,</if>
      <if test="playUrl != null">play_url,</if>
      <if test="storagePath != null">storage_path,</if>
      <if test="createdAt != null">created_at,</if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="jobId != null">#{jobId},</if>
      <if test="videoId != null">#{videoId},</if>
      <if test="bitrateLabel != null">#{bitrateLabel},</if>
      <if test="height != null">#{height},</if>
      <if test="playUrl != null">#{playUrl},</if>
      <if test="storagePath != null">#{storagePath},</if>
      <if test="createdAt != null">#{createdAt},</if>
    </trim>
  </insert>

  <delete id="deleteByExample">
    delete from media_transcode_rendition
    <if test="_parameter != null"><include refid="Example_Where_Clause"/></if>
  </delete>

</mapper>
