<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bgssai.media.common.mapper.MediaWatchProgressMapper">
  <resultMap id="BaseResultMap" type="com.bgssai.media.common.domain.MediaWatchProgress">
    <id column="id" property="id"/>
    <result column="user_id" property="userId"/>
    <result column="drama_id" property="dramaId"/>
    <result column="episode_id" property="episodeId"/>
    <result column="position_sec" property="positionSec"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>
  
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">and ${criterion.condition}</when>
                <when test="criterion.singleValue">and ${criterion.condition} #{criterion.value}</when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>

  <select id="selectByExample" resultMap="BaseResultMap">
    select id, user_id, drama_id, episode_id, position_sec, updated_at from media_watch_progress
    <if test="_parameter != null"><include refid="Example_Where_Clause"/></if>
    <if test="orderByClause != null">order by ${orderByClause}</if>
  </select>
  <insert id="insertSelective" parameterType="com.bgssai.media.common.domain.MediaWatchProgress" useGeneratedKeys="true" keyProperty="id">
    insert into media_watch_progress
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="userId != null">user_id,</if>
      <if test="dramaId != null">drama_id,</if>
      <if test="episodeId != null">episode_id,</if>
      <if test="positionSec != null">position_sec,</if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="userId != null">#{userId},</if>
      <if test="dramaId != null">#{dramaId},</if>
      <if test="episodeId != null">#{episodeId},</if>
      <if test="positionSec != null">#{positionSec},</if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.bgssai.media.common.domain.MediaWatchProgress">
    update media_watch_progress
    <set>
      <if test="episodeId != null">episode_id = #{episodeId},</if>
      <if test="positionSec != null">position_sec = #{positionSec},</if>
    </set>
    where id = #{id}
  </update>
</mapper>
