<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bgssai.media.common.mapper.MediaUserChannelMapper">
  <resultMap id="BaseResultMap" type="com.bgssai.media.common.domain.MediaUserChannel">
    <id column="id" property="id"/>
    <result column="user_id" property="userId"/>
    <result column="handle" property="handle"/>
    <result column="display_name" property="displayName"/>
    <result column="bio" property="bio"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <sql id="Base_Column_List">id, user_id, handle, display_name, bio, created_at, updated_at</sql>

  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">and ${criterion.condition}</when>
                <when test="criterion.singleValue">and ${criterion.condition} #{criterion.value}</when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>

  <select id="selectByExample" resultMap="BaseResultMap">
    select <include refid="Base_Column_List"/>
    from media_user_channel
    <if test="_parameter != null"><include refid="Example_Where_Clause"/></if>
    <if test="orderByClause != null">order by ${orderByClause}</if>
  </select>

  <select id="countByExample" resultType="long">
    select count(*) from media_user_channel
    <if test="_parameter != null"><include refid="Example_Where_Clause"/></if>
  </select>

  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="long">
    select <include refid="Base_Column_List"/>
    from media_user_channel where id = #{id}
  </select>

  <insert id="insertSelective" parameterType="com.bgssai.media.common.domain.MediaUserChannel"
          useGeneratedKeys="true" keyProperty="id">
    insert into media_user_channel
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="userId != null">user_id,</if>
      <if test="handle != null">handle,</if>
      <if test="displayName != null">display_name,</if>
      <if test="bio != null">bio,</if>
      <if test="createdAt != null">created_at,</if>
      <if test="updatedAt != null">updated_at,</if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="userId != null">#{userId},</if>
      <if test="handle != null">#{handle},</if>
      <if test="displayName != null">#{displayName},</if>
      <if test="bio != null">#{bio},</if>
      <if test="createdAt != null">#{createdAt},</if>
      <if test="updatedAt != null">#{updatedAt},</if>
    </trim>
  </insert>

  <update id="updateByPrimaryKeySelective" parameterType="com.bgssai.media.common.domain.MediaUserChannel">
    update media_user_channel
    <set>
      <if test="userId != null">user_id = #{userId},</if>
      <if test="handle != null">handle = #{handle},</if>
      <if test="displayName != null">display_name = #{displayName},</if>
      <if test="bio != null">bio = #{bio},</if>
      <if test="updatedAt != null">updated_at = #{updatedAt},</if>
    </set>
    where id = #{id}
  </update>

</mapper>
